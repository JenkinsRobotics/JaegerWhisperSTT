# JaegerWhisperSTT runtime dependencies.
#
# Engine-tier only — pins jaeger-os (framework substrate: transport,
# nodes.base, core.audio, core.voice, core.instance.setting_meta) and
# the Whisper engine's own third-party libraries. NEVER jaeger-ai — see
# module.yaml's requires_libraries for the exact probe set the
# availability gate checks.

# --- framework (path dep for local staging; pin a real published
# version range once jaeger-os has releases). PEP 508 direct reference,
# not `-e ../JaegerOS` — setuptools' dynamic-dependencies file parser
# only accepts requirement specifiers. ABSOLUTE staging path. ---
jaeger-os @ file:///private/tmp/claude-501/-Users-jonathanjenkins-GITHUB-JROS/17170042-b653-42cf-9a0b-82cc7d82616c/scratchpad/split/JaegerOS

# --- the Whisper engine itself ---
pywhispercpp>=1.4
webrtcvad-wheels>=2.0.14
sounddevice>=0.4.6
numpy>=1.26

# --- dev tooling (uncomment if doing JaegerWhisperSTT development) ---
# pytest>=8.0
# ruff>=0.4
